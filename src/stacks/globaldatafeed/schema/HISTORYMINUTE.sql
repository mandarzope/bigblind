CREATE TABLE TABLENAME (
    ID INT(11) UNSIGNED NOT NULL AUTO_INCREMENT,
    CLOSE CHAR(255) NOT NULL,
    HIGH CHAR(255) NOT NULL,
    LASTTRADETIME CHAR(255) NOT NULL,
    LOW CHAR(255) NOT NULL,
    OPEN CHAR(255) NOT NULL,
    OPENINTEREST CHAR(255) NOT NULL,
    QUOTATIONLOT CHAR(255) NOT NULL,
    TRADEDQTY CHAR(255) NOT NULL,
    PRIMARY KEY (ID),
    KEY EXCHANGE (EXCHANGE),
    KEY EXPIRY (EXPIRY),
    KEY NAME (NAME),
    KEY OPTIONTYPE (OPTIONTYPE),
    KEY STRIKEPRICE (STRIKEPRICE),
    KEY TRADESYMBOL (TRADESYMBOL),
    KEY PRODUCT (PRODUCT),
    KEY PRODUCTMONTH (PRODUCTMONTH),
    UNIQUE IDENTIFIER (IDENTIFIER)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8;